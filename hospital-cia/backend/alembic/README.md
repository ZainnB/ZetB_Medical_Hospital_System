# Alembic Placeholder

Migrations will live here once schema evolution begins. Initialize via:

```bash
cd backend
alembic init alembic
```

TODOs:
- Configure `alembic.ini` to point at `app.db.session` metadata.
- Generate baseline migration once tables stabilize.

